import React from "react";
import { Image } from "../../../../../model/types";

type ImageSettingProps = {
  imageData: Image;
};

function ImageSetting({ imageData }: ImageSettingProps) {
  return <div></div>;
}

export { ImageSetting };
